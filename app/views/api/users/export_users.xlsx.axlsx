wb = xlsx_package.workbook
wb.add_worksheet(name: "Users") do |sheet|
  sheet.add_row ['username', 'role', 'name']
  @users.each do |user|
    sheet.add_row [user.username, user.role, user.name]
  end
end
